/// <reference types="node" />
import * as vm from 'vm';
import type { FeatureFlags } from '@linaria/utils';
export declare function createVmContext(filename: string, features: FeatureFlags<'happyDOM'>, additionalContext: Partial<vm.Context>): {
    context: vm.Context;
    teardown: (() => void) | (() => void);
};
